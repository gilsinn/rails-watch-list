<br>
<br>
<br>
<h1>Show Test</h1>
<h2><%= @list.name %></h2>
<ul>
    <% @list.bookmarks.each do |bookmark| %>
      <li> <%= bookmark.movie.title %> - <em><%= bookmark.comment %></em></li>
    <% end %>
</ul>

<%= link_to "Back", lists_path %>

<%= link_to "Add New Movie",  new_list_bookmark_path(@list) %>
